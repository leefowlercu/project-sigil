# MATRIX

## Purpose

This matrix maps `sigil-web` PRD acceptance scenarios to submodule acceptance
scenarios.

## Mapping

| PRD | Scenario ID | PRD Scenario Title | Submodule Feature File | Submodule Scenario Title | Notes |
| --- | --- | --- | --- | --- | --- |
| `PRD-0100` | `SCN-0000` | `Wraps the root agent workspace route in the shared sigil-web application shell` | `sigil-web/acceptance/features/shell.feature` | `Wraps the root agent workspace route in the shared sigil-web application shell` | Shared shell contract for the root workspace |
| `PRD-0100` | `SCN-0001` | `Preserves the shared sigil-web application shell on the standalone run-detail route` | `sigil-web/acceptance/features/shell.feature` | `Preserves the shared sigil-web application shell on the standalone run-detail route` | Shared shell contract for the standalone placeholder route |
| `PRD-0100` | `SCN-0002` | `Exposes the standalone run-detail route as a placeholder workspace frame` | `sigil-web/acceptance/features/shell.feature` | `Exposes the standalone run-detail route as a placeholder workspace frame` | Standalone route remains explicitly placeholder-only |
| `PRD-0200` | `SCN-0000` | `Defaults the root workspace selection to the first available agent and reflects its canonical Agent Instance ID in the agent search param` | `sigil-web/acceptance/features/workspace.feature` | `Defaults the root workspace selection to the first available agent and reflects its canonical Agent Instance ID in the agent search param` | Root workspace selects the first available agent with its exact canonical ID |
| `PRD-0200` | `SCN-0001` | `Resolves exact canonical Agent Instance IDs and falls back when the agent search value is invalid or legacy` | `sigil-web/acceptance/features/workspace.feature` | `Resolves exact canonical Agent Instance IDs and falls back when the agent search value is invalid or legacy` | Search-param selection only accepts the exact canonical ID |
| `PRD-0200` | `SCN-0002` | `Updates root workspace selection when a different agent card is chosen` | `sigil-web/acceptance/features/workspace.feature` | `Updates root workspace selection when a different agent card is chosen` | User-driven selection updates the root workspace |
| `PRD-0200` | `SCN-0003` | `Filters agent cards by instance name or endpoint without mutating the underlying fleet` | `sigil-web/acceptance/features/workspace.feature` | `Filters agent cards by instance name or endpoint without mutating the underlying fleet` | Filter affects visibility, not fleet identity |
| `PRD-0210` | `SCN-0000` | `Loads the selected run detail in the root workspace when the selected agent has runs` | `sigil-web/acceptance/features/workspace.feature` | `Loads the selected run detail in the root workspace when the selected agent has runs` | Embedded run detail remains the root default |
| `PRD-0210` | `SCN-0001` | `Shows an empty run-detail prompt when the selected agent has no runs` | `sigil-web/acceptance/features/workspace.feature` | `Shows an empty run-detail prompt when the selected agent has no runs` | Empty detail prompt covers no-run selection |
| `PRD-0210` | `SCN-0002` | `Opens the selected run in the standalone run-detail route from the root workspace` | `sigil-web/acceptance/features/workspace.feature` | `Opens the selected run in the standalone run-detail route from the root workspace` | Root workspace links to the standalone placeholder route |
| `PRD-0220` | `SCN-0000` | `Opens the selected-agent new-run dialog as a centered modal from the root workspace` | `sigil-web/acceptance/features/workspace.feature` | `Opens the selected-agent new-run dialog as a centered modal from the root workspace` | The root workspace owns the selected-agent new-run dialog surface |
| `PRD-0220` | `SCN-0001` | `Regenerates run YAML from form edits while the dialog remains in generated mode` | `sigil-web/acceptance/features/workspace.feature` | `Regenerates run YAML from form edits while the dialog remains in generated mode` | Generated YAML remains form-driven until manual YAML edits begin |
| `PRD-0220` | `SCN-0002` | `Preserves manual YAML edits without back-populating from later form changes` | `sigil-web/acceptance/features/workspace.feature` | `Preserves manual YAML edits without back-populating from later form changes` | Manual YAML mode becomes authoritative until reset |
| `PRD-0220` | `SCN-0003` | `Restores generated YAML when the operator resets the manual YAML draft` | `sigil-web/acceptance/features/workspace.feature` | `Restores generated YAML when the operator resets the manual YAML draft` | Reset returns the draft to form-generated YAML |
| `PRD-0220` | `SCN-0004` | `Starts a new run from the root workspace and selects it after the live app-server accepts the run config` | `sigil-web/acceptance/features/live_sessions.feature` | `Starts a new run from the root workspace and selects it after the live app-server accepts the run config` | Live run starts stay rooted in the selected-agent workspace |
| `PRD-0220` | `SCN-0005` | `Surfaces live app-server invalid-run-config errors inline without closing the dialog` | `sigil-web/acceptance/features/live_sessions.feature` | `Surfaces live app-server invalid-run-config errors inline without closing the dialog` | Invalid YAML remains an inline dialog error instead of a route transition |
| `PRD-0220` | `SCN-0006` | `Defaults the run name to a read-only autogenerated slug and allows customizing or restoring it` | `sigil-web/acceptance/features/workspace.feature` | `Defaults the run name to a read-only autogenerated slug and allows customizing or restoring it` | Run-name actions preserve and refresh autogenerated slugs as specified |
| `PRD-0220` | `SCN-0007` | `Enables reasoning effort and RLM depth only when their parent toggles are enabled` | `sigil-web/acceptance/features/workspace.feature` | `Enables reasoning effort and RLM depth only when their parent toggles are enabled` | Dependent controls track their respective checkbox state |
| `PRD-0230` | `SCN-0000` | `Shows all run steps from the root node and all descendant nodes in the run-detail Steps Pane` | `sigil-web/acceptance/features/run_detail.feature` | `Shows all run steps from the root node and all descendant nodes in the run-detail Steps Pane` | Standalone run-detail Steps Pane renders all steps |
| `PRD-0230` | `SCN-0001` | `Indents run-detail steps based on their node depth in the run tree` | `sigil-web/acceptance/features/run_detail.feature` | `Indents run-detail steps based on their node depth in the run tree` | Depth-based indentation for run steps |
| `PRD-0230` | `SCN-0002` | `Displays the current step count in the run-detail Steps Pane header badge` | `sigil-web/acceptance/features/run_detail.feature` | `Displays the current step count in the run-detail Steps Pane header badge` | Header badge reflects displayed step count |
| `PRD-0230` | `SCN-0003` | `Dynamically appends new steps and updates the header count for an in-progress run in the run-detail Steps Pane` | `sigil-web/acceptance/features/run_detail.feature` | `Dynamically appends new steps and updates the header count for an in-progress run in the run-detail Steps Pane` | Live step updates for in-progress runs |
| `PRD-0300` | `SCN-0000` | `Connects a live agent endpoint only after initialize resolves a canonical Agent Instance ID` | `sigil-web/acceptance/features/live_sessions.feature` | `Connects a live agent endpoint only after initialize resolves a canonical Agent Instance ID` | Live session stays hidden until initialize yields the canonical ID |
| `PRD-0300` | `SCN-0001` | `Marks a live agent session degraded after missed heartbeats and recovers on reconnect` | `sigil-web/acceptance/features/live_sessions.feature` | `Marks a live agent session degraded after missed heartbeats and recovers on reconnect` | Heartbeat-driven connection state remains visible in the fleet |
| `PRD-0300` | `SCN-0002` | `Applies live runs snapshot and run status notifications to the selected agent workspace` | `sigil-web/acceptance/features/live_sessions.feature` | `Applies live runs snapshot and run status notifications to the selected agent workspace` | Live run notifications update workspace state |
| `PRD-0300` | `SCN-0003` | `Removes a live agent session from the fleet when the operator requests removal` | `sigil-web/acceptance/features/live_sessions.feature` | `Removes a live agent session from the fleet when the operator requests removal` | Operator removal clears the visible live session |
| `PRD-0300` | `SCN-0004` | `Rejects a duplicate live agent connection that resolves to an already-visible Agent Instance ID` | `sigil-web/acceptance/features/live_sessions.feature` | `Rejects a duplicate live agent connection that resolves to an already-visible Agent Instance ID` | Duplicate live connections are rejected with a warning toast |
